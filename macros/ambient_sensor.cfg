[gcode_macro QUERY_AMBIENT_SENSOR]
gcode:
    {% set sensor = printer["htu21d ambient_sensor"] %}
    {action_respond_info(
        "Temperature: %.2f C\n"
        "Humidity: %.2f%%" % (
            sensor.temperature,
            sensor.humidity))}